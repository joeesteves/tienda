angular.module 'Tienda' 
.config ($routeProvider) ->
	$routeProvider
	.when '/ventas',
			templateUrl: '<%= asset_path("ventas") %>'
			# controller: 'VentasIndexController'
	.when '/ventas/new',
			templateUrl: '<%= asset_path("ventas/form") %>'
			controller: 'VentasCreateController'